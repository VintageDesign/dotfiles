CompileFlags:
    Add:
        # Requires new clangd. I had good luck installing from GitHub releases into ~/.local/
        - --driver-mode=g++
    Remove:
        # - -march=*
        # - -mabi=*
        # - -mcpu=*
        - -mthumb-interwork
